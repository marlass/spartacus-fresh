{
  "git": {
    "requireCleanWorkingDir": false,
    "requireUpstream": false
  },
  "npm": {
    "publish": true,
    "publishPath": "./../../dist/storefront"
  },
  "scripts": {
    "afterBump": "cd ../.. && yarn build:core:lib"
  },
  "github": {
    "release": true,
    "releaseNotes": "cd ../.. && npm run --silent release-notes:storefront -- --from ${latestVersion} --to ${version}"
  }
}
